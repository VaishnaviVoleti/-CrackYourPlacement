class Solution{
    public:
    int sameChar(string A, string B)
    {
        // code here 
        int count = 0;
        for(int i=0;i<A.length();i++){
            if(tolower(A[i])==tolower(B[i]))
                count++;
        }
        return count;
    }
};
